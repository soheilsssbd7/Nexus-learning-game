# AGENTS.md — قوانین عملیاتی ایجنت در این ریپو

> این فایل «context دائمی» است. `docs/00-START-HERE.md` (بخش «اصول کاری برای ایجنت»)
> خواسته بود معماری همیشه در دسترس ایجنت باشد — این فایل همان نقطه‌ی ورود است.

## ۰) منبع حقیقت (Source of Truth) و اولویت اسناد

| اگر بحث سرِ … | مرجع |
|---|---|
| استک، درخت پوشه‌ها، autoloadها، قرارداد Git | `docs/01-ARCHITECTURE.md` |
| رنگ/فرم/انیمیشن/تایپوگرافی/RTL | `docs/02-ART-BIBLE.md` |
| ساختار JSON سطح، مدل بازیکن، دیالوگ، رویداد، DDL | `docs/03-DATA-SCHEMAS.md` |
| ترتیب کار، DoD هر تسک | `docs/04-BUILD-PLAN.md` |
| **تصمیمات اجرایی، فرض‌ها، انحرافات ثبت‌شده، وضعیت فازها** | `docs/05-EXECUTION-PLAN.md` + `docs/06-ENGINEERING-DECISIONS.md` |

هر تعارض بین اسناد: `03` برای داده، `02` برای ظاهر، `01` برای ساختار — و هر دو باید
در `06` به‌عنوان «رفع تعارض» ثبت شوند. **سکوت در انتخاب ممنوع؛ ثبت در ۰۶ الزامی است.**

## ۱) چرخه‌ی کار (هر تسک)

1. تسک را از `docs/04-BUILD-PLAN.md` بخوان، فرض‌های مبهم را قبل از کدنویسی در `docs/06` بنویس.
2. کد/داده را بساز.
3. راستی‌آزمایی (به همین ترتیب هزینه):
   - `gdparse` + `gdlint` روی فایل‌های `.gd` (gdtoolkit 4.5، کانفیگ `.gdlintrc`)
   - `python3 tools/validate_levels.py` برای هر تغییر در `game/data/**`
   - `godot --headless --path game --import` و اجرای GUT (اگر `.tools/godot` موجود باشد؛
     در غیر این صورت CI این کار را می‌کند — **بدون تأیید CI، تسک را تمام‌شده حساب نکن**)
   - بک‌اند: `npm --prefix backend test`
4. commit جدا با پیام `[P<phase>.<task>] <فعل امری>` (تسک = commit؛ هر فاز = یک PR به `main`).
5. وضعیت را در جدول فازهای `docs/05-EXECUTION-PLAN.md` به‌روز کن (همان commit).
6. **پیش از ورود به فاز بعد، DoD آن فاز سبز باشد.** اگر سبز نشد، فاز بعد را شروع نکن.

## ۲) بودجه‌ی ورک‌اسپیس (سقف.soft = ۲۵ مگابایت در ریپو)

- Godot/SDK/dependencies **هرگز** داخل ریپو: `.tools/`، `node_modules/`، `.venv/` (همگی ignore).
- هنر: **SVG-first** (چند کیلوبایت، مقیاس‌پذیر، هم‌راستا با «Flat Vector» Art Bible).
  PNG فقط وقتی لازم (شکست‌پذیری SVG در Godot)؛ هر PNG ≤ ۱۵۰KB، بدون atlasهای عظیم.
- تولید تصویر AI: رندر مرجع در `/tmp` بماند و **convert به SVG/وکتور دستی** شود؛
  فایل‌های خام AI (PNG 1024+) commit نشوند.
- صوت: placeholder حداکثر ۱۲۸kbps mono ogx ≤ ۵۰۰KB برای فایل؛ موسیقی Loop کوتاه (۳۰-۴۵ ثانیه).
- خروجی build (`*.apk`, `*.aab`, `builds/`) ignore است؛ APK را از artifact CI می‌گیریم.
- هر commit: `git diff --stat HEAD~1` را نگاه کن؛ یک commit > ۳MB = زنگ خطر.

## ۳) خط‌قرمزهای محصول (ریویو این‌ها را hard-fail می‌کند)

- **بدون PII**: هیچ ایمیل/نام واقعی/موقعیت/شناسه‌ی دستگاه تبلیغاتی نه در save، نه در API، نه در لاگ.
- **Aria آفلاین و قالب‌محور**؛ `FeatureFlags.LIVE_AI_ENABLED = false`. هیچ `HTTPRequest` به‌جز
  `NetworkClient` (همگام‌سازی) و هیچ SDK تحلیلی/تبلیاتی در پروژه نیست.
- **بدون قرمز تهاجمی** برای خطا؛ بدون تنبیه/شکست تنبیهی؛ زبان Aria هرگز قضاوت‌گر نیست.
- **هیچ راهنمایی جواب را نمی‌گوید** — `tools/validate_levels.py` این را خودکار چک می‌کند.
- دست‌رس‌پذیری: حداقل اندازه لمس ۴۸px، حداقل فونت گفت‌وگو ۲۴px، RTL از لایه‌ی Control.
- Godot نسخه **4.7.2-stable**؛ تغییر نسخه فقط با ثبت ADR در `docs/06`.

## ۴) امنیت/اعتبارنامه

- **هیچ توکن/رمزی در ریپو نوشته نمی‌شود** (شامل PAT گفتگوها). `export_presets.cfg` بدون
  رمز commit می‌شود؛ رمز keystore در CI از `secrets.*` و فلگ `--keystore-pass` می‌آید.
- اگر لازم شد، رازها را در `secrets` ریپو می‌گذاریم (نه در فایل). در محیط Arena احراز هویت
  GitHub از پیش آماده است و نیازی به درج دستی توکن نیست.

## ۵) سبک کد

- GDScript تایپ‌شده (`x: int`, `-> void`)؛ `class_name` برای مدل‌ها؛ **autoload فقط برای سرویس‌ها**.
- سیگنال بین سیستم‌ها فقط از طریق `EventBus` (بدون ارجاع مستقیم صحنه‌ها به هم).
- هیچ `print` بدون پیشوند ماژول؛ لاگ‌های دیباگ پشت `if OS.is_debug_build()` یا `Log.debug`.
- فایل‌ها snake_case (طبق درخت `01`؛ نام صحنه‌ها PascalCase).
- کامنت‌های توضیحیِ «چرا» به فارسی، مستندسازی API به انگلیسی — همان چیزی که در `docs` هست.

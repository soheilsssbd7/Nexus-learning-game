# CHANGELOG

قالب: Keep a Changelog ✓ نسخه‌بندی: SemVer ✓✗ **منبعِ عددِ نسخه، فایل `VERSION` است** ✓
(`VERSION` ← workflow اندروید به `version/name` می‌نشیند ✓ و `version/code` از `GITHUB_RUN_NUMBER` ✓
پس «نسخه‌ای که در فروشگاه می‌بینید» همان چیزی است که در مخزن است ✗✓ نه یک عددِ دستیِ فراموش‌شده ✓)

## [Unreleased]
- (فاز ۱۲ در جریان است ✓ فهرستِ باز: `docs/05-EXECUTION-PLAN.md` §«فاز ۱۲»)

## [0.9.0-beta.1] — 2026-09-14
### Added
- **سایتِ محصول روی GitHub Pages** ✓ (تسک ۱۲.۴): `site/**` با `tools/build_site.py` تولید می‌شود ✓
  خانه (fa + en) · راهنمای والدین · پشتیبانی · بازخورد · وضعیتِ نسخۀ وب · 404 ·
  **سیاست حریم خصوصی از `docs/privacy-policy-{fa,en}.md` رندر می‌شود** ✓ (تک‌منبع ⇒ صفحهٔ وب و
  سند هیچ‌وقت دوشاخه نمی‌شوند ✓) · اعدادِ صفحه (**تعدادِ تست/دروازه/سطح**) تایپ نشده‌اند، **محاسبه‌اند** ✓✓
- `docs/store-listing.md` ✓ (۱۲.۲ · fa + en با سقف‌های واقعیِ Play ✓ گیت دارد ✓)
- `VERSION` + `CHANGELOG.md` ✓ (۱۲.۵ · منبعِ تکِ نسخّه ✓)
- presetِ **Web** در `game/export_presets.cfg` ✓ + `android-export.yml`/workflowِ سایت برای ساختِ نسخۀ وب ✓
- دروازه‌های تازه: `check_site()` (اختلافِ تولیدشده ✗ پیوندِ شکسته ✗ ارجاعِ بیرونی/CDN ✗
  «placeholderِ بی‌علامت» ✗ سقفِ ۱۰MB فایل Pages ✗ قرمزِ خالص در CSS ✗ پالتِ ناهمسان با §۲ ✗)
  و `check_store_listing()` (سه سقفِ کاراکتری + برابریِ سن با سیاست ✓)

### Changed
- سیاستِ حریم خصوصی: ردۀ سنی از «۹-۱۲» به **9-15** اصلاح شد ✓✗ چون `docs/07` §۲ و `README` همان را
  می‌گویند ✗✓ تناقضِ ادعای سنی بین سیاست و فروشگاه، دقیقاً همان چیزی است که انطباقِ خانواده‌محور را می‌شکند ✓
- `game/export_presets.cfg`: `exclude_filter` ⇒ GUT و فایل‌های تست از بسته بیرون ✓§۱۱.۲
### Fixed
- `_md_section` در `tools/validate_levels.py` با `group(1)` بدنۀ بخش را **کلمۀ سرفصل** می‌خواند ✗✓
  (گیت تقریباً هیچ نمی‌دید) ⇒ `group(m.lastindex)` ✓✓ (ADR-067)

### Not done (صریح ⚠ تا کسی «تمام» نپندارد)
- ساختِ واقعیِ APK/AAB و نصب روی گوشی ⏳ (۱۰.۴ = L3 ✓) · تأیید `package/unique_name` توسط مالک ⚠
- آیکون/اسکرین‌شات/ویدیوی فروشگاه ⬜ (۱۲.۱ ✓ رندرِ واقعی لازم دارد) · فرم‌های Play Console ⬜ (۱۲.۳)
- آدرسِ واقعیِ پشتیبانی/بازخورد و صفحۀ «درخواست حذف» ⬜ (۱۲.۴ باقی‌مانده) · قیمت ⬜ (۱۲.۶)

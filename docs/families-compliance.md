# چک‌لیست انطباق با سیاست «خانواده‌محور» (Families) — تسک ۱۱.۲

**چرا این فایل جدا از `privacy-policy-*.md` است؟** چون سیاست، متنی است که **به والد** نشان
می‌دهیم؛ این سند، فهرستِ **کارهایی** است که باید انجام/تأیید شوند تا آن متن راست باشد ✗✓
(سیاستِ نادرست در Play یعنی رد شدن + حذف ✓ و بدترین حالت «سیاستِ راست، اپِ نادرست» است ✗✗).
هر سطر یا **دستورِ راستی‌آزمایی** دارد یا **اثرِ مشخصی که در ریپو قابل دیدن است** ✓؛
«رعایت شده ✓» بی‌مدرک در این جدول مجاز نیست ✗ (همان قاعدۀ همیشگیِ این ریپو ✓).

وضعیت‌ها: ✅ در مخزن و با گیت/تست سنجیده شده · ⚙ کد دارد، سنجشِ نهایی بیرونی است · ⬜ کارِ مالک ✓

## ۱) مخاطب و محتوای اپ
- [x] مخاطب اصلی ۹ تا ۱۲ سال، اپ «primarily child-directed» اعلام می‌شود ✓ → در Play Console:
      Target audience = children ✓ **و هر سنی که اضافه‌تر اعلام شود، تعهدِ بیشتر می‌آورد** ⚠
- [x] بدون تبلیغات، بدون خرید درون‌اپ، بدون لینک بیرونی برای کودک ✓ (تنها لینک: «بازخورد» در
      PauseMenu که **پشت قفل والدین** است ✓ تسک ۱۱.۴ ⬜ چون هنوز لینک ساخته نشده ✓✗ عمداً ⬜ است)
- [x] بدون چت، بدون نام‌های کاربری، بدون محتوای ساختۀ کاربر ✗✓ (نامِ پروفایل فقط روی دستگاه است ✓)
- [ ] محتوای بصری/متنی با رتبۀ IARC بازبینی شود (فرم IARC در Console ✓ **مالک** ⬜)

## ۲) داده: آنچه **نمی‌گیریم** (فهرستِ سیاستِ Families ✓§۹)
هر ۱۴ قلم در `docs/privacy-policy-fa.md` و `-en.md` زیرِ «هرگز جمع نمی‌شود» آمده ✓ و گیت
`check_privacy_policy()` **برابریِ فهرستِ دو زبان** را می‌سنجد ✗✓ (ترجمۀ یک‌طرفه ⇒ قرمز ✓)
و به‌علاوه اینکه هیچ‌کدام در بخش «جمع می‌شود» ظاهر نشوند ✓ (قفلِ درون‌بخشی ✓✓):

| قلم | وضعیت | اثر/راستی‌آزمایی در مخزن |
|---|---|---|
| IMEI / MEID | ✅ | هیچ API این‌چنینی در `game/scripts/**` نیست ✗ (جاروی گیتِ `check_text_hygiene` روی `game/**` ✓) |
| AAID / شناسۀ تبلیغاتی | ✅ | هیچ SDK تبلیغاتی در پریست/manifest نیست ✓ `check_export_config` نام `AD_ID` را **خطا** می‌داند ✓✓ |
| شماره سریال سیم‌کارت / دستگاه | ✅ | نه مجوز `READ_PHONE_STATE` ✓ نه کد ✗ |
| موقعیت جغرافیایی (precise/coarse) | ✅ | `ACCESS_FINE/COARSE_LOCATION` ممنوع در `check_export_config` ✓✓ |
| MAC / SSID / BSSID | ✅ | هیچ دسترسی شبکه‌ای جز `HttpRequest` نداریم ✓ (allowlistِ دومیری: `NetworkClient` و `LiveAIProvider` ✓✓ تستِ ۱۸ِ ۹.۶) |
| مخاطبین / دوربین / میکروفون | ✅ | مجوزها = فقط `INTERNET` + `VIBRATE` ✓ `game/android/permissions.xml` ✓ و workflow با `apkanalyzer` روی **بستهٔ واقعی** چک می‌کند ✓✓ |
| دادهٔ عملکردِ دستگاه (باتری/CPU/حافظه) | ✅ | آنالیتیکس = ۶ نوعِ رویدادِ آموزشی ✓ (`HANDLERS` قفلِ دوزبانه ✓ ADR-065) |
| متن/صدای کودک | ✅ | `hint_shown` فقط `hint_id`؛ تستِ ۵ با `JSON.stringify` اثبات می‌کند ✗✓ (ADR-065 ✓) |
| UGC بدون نظارت | ✅ | UGC نداریم ✓ |
| دادهٔ والد | ✅ | پنل والدین محلی است و چیزی نمی‌پرسد ✓ (خروجی = کپی JSON به کلیپ‌بورد ✓§۶.۵) |

## ۳) Data Safety فرم (پاسخ‌های اعلامی ✓ باید با کد یکی باشد ✗✓)
- [x] «No data collected» **نه** ✗— صادقانه: می‌گوییم «data is collected» برای **App functionality**
      (منحنی یادگیری) ✓ و «not shared with third parties» ✓ (ما خودمان پردازش می‌کنیم ✓ ADR-010)
- [x] «Data is encrypted in transit» ✓ (HTTPS در محیط تولیدی ⚙ تا deploy نهایی ⬜ چون آدرسِ هاست
      هنوز تعیین نشده ✓ — این را نمی‌توانم در مخزن حل کنم ✗✓)
- [ ] «Users can request data deletion» ⇒ **بله، و باید واقعی باشد** ⚠: مسیرِ حذف در سیاست نوشته
      شده (`privacy-policy` §«پاک کردن همه‌چیز») ولی **پشتیبانِ انسانی‌اش** یک ایمیل/فرم است که
      در تسک ۱۲.۴ ساخته می‌شود ⬜ ⇒ **تا آن تسک، این چک‌لیست اجازهٔ ارسال ندارد** ✗✓ (این سطر،
      گلوگاهِ واقعیِ انتشار است، نه تشریفات ✓)
- [ ] هرگونه «ads SDK self-certification» ⇒ **بی‌مورد، چون تبلیغات نداریم** ✓ (پاسخ = No ✓)

## ۴) فنی/بسته
- [x] `targetSdk = 36` ✓ (`docs/smoke-checklist.md` §۲ + `apkanalyzer` در `android-export.yml` ✓✓
      الزام Google Play از ۳۱ اوت ۲۰۲۶ ✓ ADR-009 ✗ تمدیدِ خواسته‌شده تا ۱ نوامبر را **نمی‌خواهیم**)
- [x] `minSdk = 24` ✓ (پوشش گوشی‌های ارزان ✓§۰)
- [x] فایلِ اضافهٔ dev در بسته نرود ⚙ → `exclude_filter` در `export_presets.cfg` (الگوهای
      `addons/gut/*` و `tests/*`) ✓✗ **پوششِ خطا در این بند واقعی است**: بدونش، GUT و ۴۹ فایلِ
      تست داخل APK می‌روند ⇒ حجمِ بی‌دلیل ✓ و «کدِ بی‌مصرف» که اسکنرهای فروشگاه دوست ندارند ✗
      (تأییدِ نهاییِ الگوها با اولین export واقعی ⚙ ✓ ادعای بی‌آزمون نداریم)
- [x] `keep_screen_on=true` ✓ (بند ADR-009 ✓ یادگیری طولانی‌مدت؛ برای کودکِ در حال تمرین ✓)
- [ ] اجازهٔ نصبِ «منبع نامعلوم» لازم نیست (Play ✓)؛ اگر بتا خارج از Play پخش شود ⚠ سیاست عوض می‌شود ⬜

## ۵) امضا پیش از ارسال build ⚠ (بند ۱۱.۲ DoD: «تکمیل‌شده و امضاشده»)
| بند | مسئول | وضعیت | یادداشت |
|---|---|---|---|
| §۱ مخاطب و IARC | مالک | ⬜ | فرم در Play Console ✓ بیرون از مخزن |
| §۲ داده (۱۰ سطر) | کد + گیت | ✅ | `check_privacy_policy` + `check_export_config` + ADR-065 |
| §۳ Data Safety (۴ سطر) | مالک + کد | ⬜ | **گلوگاه:** سطرِ حذف، نیازمند ۱۲.۴ ✓ |
| §۴ فنی/بسته | کد + دستگاه | ⚙ | `apkanalyzer` روی artifact ✓ + یک بار اجرای دستیِ workflow ✓ |

**حکمِ این سند:** سه ⬜ باقی‌مانده هیچ‌کدام «کد‌نشدنی» نیستند ✗✓ بلکه **بیرون از مخزن‌اند** (فرم‌ها،
هاست، آدرسِ تماس ✓) ⇒ تسک ۱۱.۲ از نظرِ «چک‌لیستِ تکمیل‌شده» انجام شده ✓ و از نظرِ «امضا پیش از ارسال
build» منتظرِ ۱۲.۴ است ✓✗ این تفاوت را پنهان نمی‌کنیم ✓

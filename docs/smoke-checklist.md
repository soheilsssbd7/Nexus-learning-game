# چک‌لیست smoke فاز ۱۰ — تسک ۱۰.۵ (افزودۀ ایجنت؛ سند ۰۴ §۱۰.۵)

**قاعده:** هر سطر یا **دستوری** دارد که در ۳۰ ثانیه اجرا می‌شود، یا **عدد** ✓؛ «انجام شد ✓» بی‌عدد
در این چک‌لیست مجاز نیست ✗✓ (همان درسی که در ۹.۱ گرفتیم: «لاگِ موفق» دلیلِ باز بودنِ پورت نیست ✓✓).
مسیرها از خودِ کداند: `user://player_model.save` · `user://ui_settings.json` · `user://device_token.save` ✓

## ۱) مسیرِ اصلی (DoD ۱۰.۵، کلمه‌به‌کلمه)

- [ ] **نصبِ تازه**: `adb install -r` بی‌داده‌ی قبلی (`adb shell pm clear <pkg>` بعد از نصب ✗ فقط برای این تست ✓) ⇒ صفحه باید **Onboarding** باشد، نه نقشه ✓ (`run/main_scene` ✓§۶.۲)
- [ ] **Onboarding**: یک پروفایل با نامِ نمایشی ساخته شد ⇒ فایل متناظر زیر `user://profiles/` (یا حالتِ تک‌نفره: `user://player_model.save`) روی دیسک آمد ✓ — با `adb shell run-as <pkg> ls -l files/` ببین، نه با چشم ✗
- [ ] **۳ سطح اول** (tier1/level_1_01..03) تا برد ✓؛ زمان هر سطح در `PlayerModel` ثبت شود (`avg_solve_sec` غیرصفر ✓§۲)
- [ ] **آریا حداقل یک‌بار**: عمداً در یک سطح ۲ بار اشتباه کن ⇒ `STRUGGLE_ATTEMPTS=2` راهنما را صدا می‌زند ✓§۴؛ پیام آریا باید **بدون رقمِ جواب** باشد ✗✓ (`check_answer_leaks` ✓ + چشمِ تو ✓)
- [ ] **ParentDashboard**: از Pause ⇒ قفلِ ضربان/سوال (ADR-۰۴۷) ⇒ نمودار `MasteryChart`، `error_patterns` بزرگ‌تر‌اول، و خروجیِ JSON با «کپی» ✓؛ هیچ‌جا **نامِ واقعی/ایمیل/موقعیت** نباشد ✓§۹
- [ ] **بستن و بازکردن مجدد** (از Recent، نه suspend): سطح باز‌شده، Elo، آمار و تنظیمات صدا **عیناً** برگردند ✓✗ اگر یکی عوض شد ⇒ save در `AboutToQuit` نوشته نشده (فاز ۱/۶ را ببین ✓)

## ۲) پلتفرم/بسته (پلن Bِ نبودِ Android SDK در سندباکس ⇒ این‌ها روی **دستگاهِ تو** سنجیده می‌شوند ✓)

- [ ] `minSdk 24` / `targetSdk 36` ✓ (`apkanalyzer manifest print app.apk | grep -E "minSdk|targetSdk"`) — API 36 از ۳۱ اوت ۲۰۲۶ برای اپ‌های تازه الزامی است ✓ ADR-009
- [ ] **edge-to-edge**: در Android 15+ نوار وضعیت/ناوبری روی بازی نیفتد ⇒ `DisplayServer.get_display_safe_area()` + `get_visible_rect()` ✓ (نام‌های کلیدِ پروژه را اختراع نکن ✗ — `PHANTOM_API` گیت دارد ولی برای تنظیماتِ پروژه نه ✓) (تستِ خودکارِ محلی: `test_a11y_scenes.gd` کفِ ۴۸dp را می‌سنجد؛ **برخورد با ناوبری** فقط روی دستگاه ✓)
- [ ] **predictive back**: دکمه/حرکتِ «برگشت» ⇒ `NOTIFICATION` به‌جای قتلِ ناگهانیِ صحنه؛ از Pause و نقشه هر دو را امتحان کن ✓ (نشتیِ صحنه: `_draw()` نباید هر فریم `StyleBox`/`PackedStringArray` تازه بسازد ✓)
- [ ] مجوزها **دقیقاً** `INTERNET` + `VIBRATE` ✗ بدون مخاطبین/میکروفون/دوربین/موقعیت (`apkanalyzer manifest print` ✓§۹/ADR-010)
- [ ] `Vibrate` واقعاً کار کند: در دستگاهِ لرزاننده، پاسخِ اشتباه ⇒ لرزش ✓ (بی‌این، §۷ فقط عدد است ✗)
- [ ] اندازه‌ی بسته: `AAB ≤ 24MB` و `APKِ install-time ≤ ~30MB` ✓§۰ (بودجۀ هنر: ≤ ۴MB ✓؛ اگر پرید، اول SVGها را `svgo` کن نه اینکه رزولوشن را ببرم ✗)
- [ ] شروعِ سرد روی ارزان‌ترین گوشیِ موجود `< 4s` تا MainMenu ✓ عدد را بنویس: `adb shell cmd package resolve-activity -c android.intent.category.LAUNCHER <pkg>` ⇒ بعد `adb shell am start -W -n <pkg>/<activity> | grep TotalTime` ✓ (نام Activity را از خودِ بسته بخوان، حدس نزن ✗)
- [ ] **60 دقیقه** بازی ⇒ `adb shell dumpsys meminfo <pkg>` دو بار (ابتدا/انتها) ⇒ رشدِ پیوستۀ PSS = نشتی ✗✓

## ۳) آفلاین و شبکه (حلقه‌بستنِ DoD ۹.۶ روی دستگاه ✗✓)

- [ ] حالت هواپیما ⇒ بازی کاملاً قابل‌بازی، بدون کرش، بدون هیچ پیامِ خطا به کودک ✓§۹ (صف در حافظه است ✓؛ مرگِ بازی = صف می‌رود ← بدهیِ ثبت‌شدۀ ADR-064 ✓)
- [ ] آفلاین 30 دقیقه ⇒ پس از وصل شدن، صف **خالی** شود (`adb logcat -s nexus:* | grep -E "network|flush"`) ✓✓؛ اگر صف ماند، `stats().last_error` را بخوان — «no_player_for_token» یعنی ۴۰۱ِ بی‌mint ✗✓ (همان باگی که CI گرفت ✓)
- [ ] بک‌اند بسته/۵۰۳ ⇒ عقب‌نشینیِ تصاعدی و **سقف ۶ تلاش**، نه چرخشِ داغ ✓ (باتری!: `dumpsys battery` قبل/بعد ⇒ مصرفِ بی‌دلیل ⇒ cooldown کار نمی‌کند ✗)
- [ ] `cloud_sync_enabled` خاموش ⇒ **صفر** درخواست (`tcpdump`/`logcat` ✓) ⇒ این همان دلیلی است که Data Safety ما می‌تواند «هیچ داده‌ی شخصی» اعلام کند ✓§۱۱
- [ ] در `logcat` هیچ‌گاه این‌ها دیده نشوند: `IMEI`, `AdId/AAID`, `MAC`, `SSID`, `BSSID`, `serial`, موقعیت ✓ (ADR-009/010 — سیاست Families)

## ۴) ثبتِ نتیجه (این جدول، خودِ «گزارش L3» است ✓ DoD ۰.۲/۱۰.۴ همین را می‌خواست)

| مورد | دستگاه | Android | عدد مشاهده‌شده | نتیجه | issue (در صورت شکست) |
|---|---|---|---|---|---|
| §۱ بستن/بازکردن ⇒ save | | | | | |
| §۲ مجوزها دقیقاً دو تا | | | | | |
| §۳ flush بعد از وصلِ مجدد | | | | | |

**امضای مالک برای عبور از فاز ۱۰:** همه‌ی سطرهای بخش ۱ و ۳ سبز ✓ + یک نصبِ واقعی روی گوشیِ ارزان ✓
(بخش ۲ روی emulator **کافی نیست**: edge-to-edge و لرزش، emulator-نشناس‌اند ✗✓)
